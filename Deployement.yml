apiVersion: apps/v1
kind: Deployment
metadata:
  name: appbuilder-application-service
  labels:
    app: appbuilder-application-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appbuilder-application-service
  template:
    metadata:
      labels:
        app: appbuilder-application-service
    spec:
      containers:
      - name: appbuilder-application-service
        image: oumaima12/application_service:V5
        ports:
        - containerPort: 9882
---
apiVersion: v1
kind: Service
metadata:
  name: appbuilder-application-service-service
spec:
  selector:
    app: appbuilder-application-service
  ports:
  - protocol: TCP
    port: 9882
    targetPort: 9882
